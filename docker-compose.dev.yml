services:
  hpc-docs-dev:
    image: node:24-alpine
    container_name: hpc-docs-dev
    working_dir: /app
    volumes:
      - .:/app
      - /app/node_modules
    ports:
      - "3000:3000"
    command: sh -c "npm install && npm run start -- --host 0.0.0.0"
